Phase 1: Split the repo into two clean utilities
Create a dedicated MinAndRest script
Create a dedicated Skyline script
Keep the launcher and startup flow unchanged
Keep both scripts in the repo under the same project

Phase 2: Finish MinAndRest
Keep Ctrl+Alt+M for minimize active window
Keep Ctrl+Alt+R for restore last minimized window
Repeated restore while already restored does nothing
Restore maximized state correctly
Add monitor fallback logic:
use original monitor if available
otherwise use primary or current fallback monitor

Phase 3: Build Skyline v1 core
Scope for this phase only:
- Ctrl+Alt+T: track active window
- Ctrl+Alt+Shift+T: untrack active window
- Ctrl+Alt+L: view tracked list
- The list window should re-focus when opened again from another window
- If the list is open and a tracked/untracked action happens elsewhere, the list should refresh so the user sees the new state
- Add Ctrl+Alt+X to clear all tracked windows
- Ignore the list window itself and any temporary UI windows from being tracked
- Visible tooltips for all successful or skipped actions
- Keep the tracked list as runtime memory only
- Do not include preset save/recall yet; that needs persistence and app registry support
- The list should show tracked windows in newest-first order

Phase 4: Add app registry + persistence [COMPLETE]

Phase 4A: Define the storage model and file layout [DONE]
- ✓ Create a Data folder beside the script
- ✓ Save app registry entries as name + executable path
- ✓ Save preset entries separately from the registry
- ✓ Keep tracked runtime state in memory only
- ✓ Validation tooltip when registry and preset files are created

Phase 4B: Build the app registry [DONE]
- ✓ Add a RegistryPath value and a registry read/write helper with proper JSON format
- ✓ Save app names and executable paths as a stable record
- ✓ Prompt user with the app label to reselect if path is missing
- ✓ Show validation tooltip when app is registered
- ✓ FIX: Unescap backslashes when reading from JSON to prevent double-escaping

Phase 4C: Save a preset slot [DONE]
- ✓ Save the current tracked set into the selected preset slot
- ✓ Persist each tracked app as a name + executable path pair
- ✓ Replace the slot value instead of appending
- ✓ Register each app in the registry during save
- ✓ Show validation tooltip after saving with app count
- ✓ FIX: Preserve existing presets when saving new ones
- ✓ Refresh list window after save to show persisted state

Phase 4D: Load a preset slot [DONE]
- ✓ Read the selected preset slot
- ✓ For each saved app entry, check if it is already open
- ✓ If it is open, use that window
- ✓ If it is not open, use the saved path directly (launch it)
- ✓ If the saved path is invalid or missing, prompt user to locate it
- ✓ Replace previous tracked set with preset set after load
- ✓ Show validation tooltip after activation with count found
- ✓ Refresh list window after load

Phase 4E: Validation and cleanup [DONE]
- ✓ Temporary debug tooltips in place for validation
- ✓ Multiple presets can be saved and loaded independently
- ✓ App registry persists across restarts
- ✓ Preset data uses proper JSON format
- ✓ Backslash escaping is correct (no double-escaping)
- Note: Debug tooltips left in place for now per user request (will clean in final phase)

Phase 5: Build the master UI and mini list UI
0 opens the main UI
Section 1: tracked windows list view
Section 2: preset list with save/load/edit buttons
Section 3: saved app registry entries with add/remove actions
The tracked list should become a lightweight mini UI instead of a MsgBox, and it should stay open, refresh live, and refocus correctly when reopened
This is now the preferred UI direction for the tracked-list behavior and is part of the Skyline UI layer

Phase 6: Add preset editing
Each preset gets:
load current tracked set into this preset
edit preset contents
Editing should allow:
app selection from saved registry
monitor selection
layout mode selection
For the first version, keep layout modes simple:
full monitor
left half
right half
custom x/y/w/h
This avoids overcomplicating the first pass

Phase 7: Monitor geometry handling
Save monitor ID and geometry metadata
On recall:
if monitor still exists, restore there
otherwise use best available monitor
Keep the fallback logic simple and predictable

Phase 8: Final polish and docs
rename the minimize/restore file to something clean like MinAndRest.ahk
update the README after MinAndRest is validated
update the README again after Skyline is finished


Problems/Notes to Adress Later:
When list is run, if the menu goes out of focus, callign list again should bring 
that same window into focus
when list is called, that menu should refresh upon tracking/untracking
Should Skyline get its own directory in scripts, to group the main ahk script
and the Data directory? Or, if this same data might be used in other scripts 
later down the line, could it make sense to rename that more generaly instead
of skyline...